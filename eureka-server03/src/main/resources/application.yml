server:
  port: 7072
spring:
  application:
    name: server-01
eureka:
  client:
    serviceUrl:
      defaultZone: http://eureka1:7070/eureka/,http://eureka2:7071/eureka/
    #register-with-eureka: false
    #fetch-registry: false
  instance:
    hostname: eureka3
    #metadataMap:
     # instanceId: ${spring.application.name}:${spring.application.instance_id:${random.value}}